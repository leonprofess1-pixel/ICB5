## 데이터셋 위치
- 데이터 경로는 `./causal-inference/data/wage.csv`를 기준으로 할 것.

## 관련 파일 저장 위치
모든 작업 결과는 causal-inference 폴더 안에만 생성 할 것
데이터시각화 내용도 causal-inference/images 폴더 안에 생성 할 것

# Role
인과추론 실습을 위해, 실행만 하면 데이터 분석부터 마크다운 리포트 생성까지 완료되는 '마스터 파이썬 코드'를 작성해줘.

# 분석 목표 
educ(교육연차) 가 wage(임금) 에 미치는 영향을 분석. 
교란 변수를 찾아, 교란 변수를 통제한 상태로 평균을 이용해서 분석.

- 마크다운에서 보여지는 이미지 경로는 ./causal-inference/images/ 하위에 파일명을 지정해서 가져오게 할 것 
- 시각화 이후 시각화에 대한 해석도 진행할 것.
- 분석 결과물은 md 파일과, pdf 파일로 모두 저장할 것.

## 기술 통계 테이블 형식 유지
- `df.describe()` 결과를 마크다운으로 변환할 때 테이블이 깨지는 것을 방지하기 위해, `to_markdown()` 대신 `to_string()` 메소드를 사용합니다.
- 변환된 문자열을 마크다운 파일에 쓸 때, 아래와 같이 코드 블록(```)으로 감싸서 원본 형식이 그대로 유지되도록 합니다.

# Task
아래의 모든 요구사항을 포함하는 단 하나의 .py 파일을 작성할 것.

## 단계별 분석 진행
- 데이터 로드하고 확인하는 .py 코드 작성할 것
- 모든 단계 별 분석 내용은 마크다운 파일로 출력하도록 할 것 
- 막대그래프로 시각화 한 내용은 하단에 교차표나 피봇테이블로 해당 내용을 꼭 출력할 것 
- 시각화 내용도 마크다운 파일에서 볼 수 있게 하고 저장된 파일 경로를 지켜서 작성할 것 
1. **환경 설정**: 
   - `./causal-inference/` 및 `./causal-inference/images/` 폴더가 없으면 자동으로 생성할 것.
   - 데이터 경로는 `./causal-inference/data/wage.csv`를 기준으로 할 것.

2. **데이터 분석 로직 **:
   - 1단계: EDA (상위 5개 행, 기술통계량 요약)
   - 2단계: educ 가 wage 에 미치는 인과효과를 추정. 
   - 3단계: educ, wage 의 인과관계에서 교란 변수로 작용할 수 있는 요인 파악 
   - 4단계: 1단계에서 찾은 교란 변수들을 통제하여 공정한 비교(apple-to-apple comparison) 가 가능하도록 세팅.
   - 5단계: 공정한 비교를 통해 educ 가 wage 에 미치는 정량적 영향도 파악하고, 2단계에서 educ -> wage 인과 효과를 바로 추정했을 때와 차이 비교.
   
3. **리포트 자동 생성 (가장 중요)**:
   - 파이썬의 `f.write()` 또는 `pathlib`을 사용하여 `regression_analysis_report.md` 파일을 생성할 것.
   - 분석 중 생성된 모든 그래프(최소 10개 이상)를 `images/` 폴더에 저장하고, 마크다운 리포트 안에서 `![이미지](./images/파일명.png)` 문법으로 호출할 것.
   - 분석 결과(statsmodels의 summary 테이블 등)를 텍스트로 변환하여 마크다운 코드 블록 안에 포함할 것.

# Constraints
- 시각화 시 한글 깨짐 방지 설정을 포함하거나 영문으로 작성할 것.
- 코드에는 주석을 상세히 달아 초보자가 이해하기 쉽게 해야 하며, 단순 평균 비교 외에 복잡한 통계기법(다중 회귀 분석 등) 을 사용하지 말 것.
- 분석 결과물은 .md 파일 하나에 모든 내용이 집약되어야 하며, 모든 해석은 반드시 한글로 작성할 것.

# Output
위 요구사항을 모두 충족하는 '전체 파이썬 코드'만 출력해줘.