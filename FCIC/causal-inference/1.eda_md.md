## 데이터셋 위치
- 데이터 경로는 `./causal-inference/data/wage.csv`를 기준으로 할 것.

## 관련 파일 저장 위치
모든 작업 결과는 causal-inference 폴더 안에만 생성 할 것
데이터시각화 내용도 causal-inference/images 폴더 안에 생성 할 것

## 기본 EDA 진행
- 데이터 로드하고 확인하는 .py 코드 작성할 것
- 모든 EDA 내용은 마크다운 파일로 출력하도록 할 것 
- 막대그래프로 시각화 한 내용은 하단에 교차표나 피봇테이블로 해당 내용을 꼭 출력할 것 
- 시각화 내용도 마크다운 파일에서 볼 수 있게 하고 저장된 파일 경로를 지켜서 작성할 것 
- 데이터 시각화시 10개 이상 그래프 시각화 하고 해당 내용에 대해서도 자세히 마크다운 문서에 작성할 것 
- EDA 내용은 마크다운 파일 1개에 모두 정리해서 작성할 수 있게 할 것 
- 마크다운에서 보여지는 이미지 경로는 ./causal-inference/images/ 하위에 파일명을 지정해서 가져오게 할 것 
- 시각화 이후 시각화에 대한 해석도 진행할 것.
- 분석 결과물은 반드시 한글 md 파일로 모두 저장할 것.

## 기술 통계 테이블 형식 유지
- `df.describe()` 결과를 마크다운으로 변환할 때 테이블이 깨지는 것을 방지하기 위해, `to_markdown()` 대신 `to_string()` 메소드를 사용합니다.
- 변환된 문자열을 마크다운 파일에 쓸 때, 아래와 같이 코드 블록(```)으로 감싸서 원본 형식이 그대로 유지되도록 합니다.

## 가상환경
.venv 해당 경로에 있는 가상환경만 사용할 것
